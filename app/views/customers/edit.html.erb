<h1>Editing customer</h1>

<% form_for(@customer) do |f| %>
  <%= f.error_messages %>
		<%= render :partial => 'form' %>
  <p>
    <%= f.submit "Update" %>
  </p>
<% end %>

<%= link_to 'Show', @customer %> |
<%= link_to 'Back', customers_path %>
